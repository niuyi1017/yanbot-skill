# 2026-06-01 — Skill 开发

## 分支管理
- 从 main 创建 `feature/multi-platform-and-docs` 分支

## SKILL.md 更新
- 新增 3 个平台适配：**VS Code Copilot**、**Trae IDE**、**通义灵码**
- VS Code Copilot 关键差异：使用 `"servers"` 键（非 `"mcpServers"`），HTTP 类型需加 `"type": "http"`
- Trae IDE 和通义灵码：通过 IDE UI 界面配置，无直接文件路径
- Step 2.5 激活指引新增 3 个平台的操作步骤
- Common Issues 新增 5 条平台特定错误排查

## check_yanbot_mcp.py 更新
- 新增 VS Code Copilot 检测（用户级 mcp.json 路径，json_path 为 `["servers"]`）
- 新增 Trae IDE 检测
- 通义灵码因配置存储在 IDE 内部，脚本无法可靠检测，仅在 SKILL.md 中提供 UI 指引
- 脚本验证通过：WorkBuddy OK，端点可达（HTTP 400 = server responded）

## README.md 重写（两轮迭代）
- 第一轮：从开发者视角 → 用户视角三段式（是什么/怎么装/能做什么）
- 第二轮（WiLL 反馈）：核心定位从"MCP 初始化工具"调整为"研bot 考研数据查询"
  - 标题改为「研bot」，tagline 聚焦"查考研数据"
  - 第一屏是能力展示（5 大场景 + 实战对话示例），安装内容后置
  - 平台表精简为名称+状态，去掉配置路径细节
  - "如何安装"从主导航降级为二级段落，保持简短

## 已同步
- SKILL.md + check_yanbot_mcp.py + README.md 已同步到 `~/.workbuddy/skills/yanbot-mcp-setup/`

## 第三轮优化：安装即配置（一步完成）
- **SKILL.md** 新增 `Auto-Setup on Install` 章节：Skill 加载时自动执行 Step 0→3 全流程，不再需要用户分开说"安装"和"配置"
- 触发词扩展：`"安装 研bot"` / `"install yanbot"` / `"配置研bot"` 任意一个即可触发全流程
- **README.md** "如何安装"改为一步：
  - WorkBuddy：说 `"安装 研bot"` 即完成（之前需要两步）
  - 其他平台：提供一键复制命令，发给任何 AI Agent 即可完成安装+配置
- description 重写：从"配置工具"定位改为"安装并配置，一步接入考研数据查询"
